> **Authority: IMPLEMENTATION STATUS**
> This file reports verified implementation state. It does not create gameplay rules.

# Rival Saga Implementation Status

## Action Phase Balance Update

Rival Saga now has one supported playable Action Phase runtime. `action-phase-v2-real-series` remains the stored identifier for the current Route-era ruleset and mounts the Route Action workspace for all supported games. The retired V1 Action Phase is preserved only in Git history and archive branches; production no longer provides a V1 compatibility path or guarantees that V1 saves can continue as V1.

The retired V1 runtime is no longer a maintenance or compatibility target. The isolated V2 Route Encounter engine lives under `versions/next-action-phase/` and the browser mount now adapts its current Route Action, opportunity, result, and acquisition contract into persisted `state.v2` buckets. Fresh generated Routes 1-9 each independently roll and persist hidden 20-30 ordinary resident populations from the approved weighted Route distributions, plus 2 hidden permanent Premium Residents from Ultra Elite, Master, and Master Elite tiers. Existing persisted Route populations, including older 24-resident Routes, remain authoritative and are not regenerated. The route system persists hidden Route Quality for Routes 3-9, creates exactly one opportunity per normal Route Action, draws and publicly discovers permanent residents with reduced encounter weight for Premium Residents, preserves same-result reroll revisions on the same Route, checks personal duplicate ownership, maintains per-player private reveals, applies exact Battle Tier suppressions for Repel, mounts source-agnostic Route Effect records for private/table reveal and temporary injection operations, adds exactly four temporary Primary-Type encounter options to one pending opportunity using weighted tier rolls capped at Master, enforces Extra Encounter progression limits, constrains Master Ball to public/private known permanent residents, and finalizes acquisition records. Player-specific Duplicate ON/OFF preferences now persist only sparse OFF records by `playerId -> routeId -> residentId`; OFF can be set only for permanent residents known and owned by that player, filters only that player's standard random draw and random reroll pool, leaves explicit mechanics such as Master Ball/reveal/Repel unaffected, and does not mutate resident populations, Premium slot metadata, public discoveries, or another player's encounter pool. Extra Encounter's mounted browser contract purchases the approved $2,500 freely purchasable/storable Token as an exact inventory record, preflights the acting player's authoritative encounter pool before consumption, then consumes that exact record for one additional Route opportunity without spending a V2 Action.

The V2 Route engine now adapts real Rival Saga Pokemon data using `pokemon-balance-tiers.js` for approved Battle Tiers and `pokemon-build-data.js` for type/species/regional metadata. The current eligible Route catalog contains 952 entries: LC 339, LC Elite 135, Safari 159, Poke 85, Great 70, Ultra 69, Ultra Elite 51, Master 32, and Master Elite 12. Valid Elite tiers outside ordinary or Premium Resident generation are reported as excluded. Existing repository aliases resolve `Basculegion-Blue` to `basculegion-female` and `Basculegion-Red` to `basculegion-male`; the real-data audit now reports zero malformed source rows and 26 alias resolutions. Regional Variance is active for Kanto, Johto, Hoenn, Sinnoh, Unova, Kalos, Alola, Galar, and Paldea Series generation. For each legal Battle Tier pool after same-Route duplicates are removed, every candidate receives universal-pool weight `0.5`, and each featured/current-generation candidate also receives featured-pool bonus `0.5 * (legalCandidateCount / featuredCandidateCount)`. Cross-Route repeat suppression multiplies subsequent placements by `0.25 ** priorPlacementCount`. Premium Residents do not apply Regional Variance for now, and Hisui-form regional identities remain explicitly unresolved rather than treated as a tenth Series region. Isolated Series initialization stores Route state at `state.v2.routeEncounterBySeriesId[seriesId]`, preserves existing state on repeated initialization, and normalizes JSON-safe reload state without rerolling resident populations or Route Quality. `npm.cmd run audit:v2-routes` runs the developer real-data generation audit and verifies regional elevation over unbiased same-tier baselines.

Route Exploration is now the first implemented isolated V2 Action type. `route-exploration` validates player, Series Route state, chosen Route, available V2 Action budget, and stable Action ID; commits exactly one reversible Action spend; creates exactly one normal Route opportunity; resolves one Route result; finalizes one acquisition; creates one normal owned Pokemon record in `state.pokemonRecords`; links it to the owner's `pokemonIds`; and settles only after acquisition exists. The persisted causal chain is Action ID -> Spend ID -> Opportunity ID -> Result ID -> Acquisition ID -> Pokemon record ID. Duplicate Action commits, duplicate encounter draws, and duplicate acquisition requests return existing records without duplicating spends/opportunities/Pokemon. Reload tests cover committed-before-roll, unresolved result, rerolled result, acquired-but-unsettled, and fully settled states. Undo is not implemented, but the exact IDs needed for a later causal reversal are now recorded.

The mounted V2 Route workspace exposes public Route discoveries, player-private known residents only to that player, encounter capabilities, pending opportunity selectors, safe visible-effect summaries, exact Reroll Token use, personal duplicate free rerolls, player-scoped Duplicate ON/OFF controls, Extra Encounter purchase/use, Repel exact-tier suppression, Primary-Type injection from the Encounter Rail, and Master Ball opportunity resolution. Repel capabilities now derive available Battle Tier choices from actual unsuppressed permanent Route residents, including Elite and Premium tiers when at least 5 exact-tier residents are eligible. The V2 Route Browser intentionally exposes each Route's total encounter-slot count and 2 reserved Premium Resident slots as public UI information; unrevealed identities still render only as generic mystery slots. Hidden Route populations, hidden resident IDs, Route Quality, encounter weights, RNG seed, suppression resident IDs, temporary candidate IDs, and Route Effect internals remain out of the render path. Browser-runtime coverage now exercises normal Action acquisition, Premium Resident slot persistence, Reroll Token acquisition, personal duplicate reroll, player-specific Duplicate OFF filtering/re-enable across backend-style reload and dormant ownership loss, Extra Encounter purchase/use/acquisition plus empty-pool and future-Route rejection safety, base-tier and Elite-tier Repel before later encounters, private reveal into Master Ball eligibility, table reveal sharing, exactly-four Primary-Type injection with 75% natural-tier-or-lower, 20% +1-tier, and 5% +2-tier rolls with downward-only fallback, temporary-result duplicate reroll/acquisition, repeated invocation, and multi-player ownership isolation. Focused validation currently includes `node --check app.js`, `node --check server.js`, `npm.cmd run test:v2-route-browser`, `npm.cmd run test:v2-route-runtime`, `npm.cmd run test:v2-routes`, and `npm.cmd run audit:v2-routes`.

The Action Phase location runtime now uses a shared consolidated Battle Tier helper for Ticket families, Graveyard Destroy Value, Silph Co. R&D prices, and Dragon's Den prices. Game Corner uses the finalized Ticket prices and Slot Machine distribution. Department Store is a persisted once-per-Gym operation over the full Item/TM catalogs; Move Dojo and active Item/TM Point discounts are removed. Clearance rolls persist with the visit and purchased entries carry `clearance` plus `finalSale` markers.

Day Care and Dragon's Den use roster-preserving unavailable flags and idempotent automatic next-Gym returns. Day Care resolves +3 Levels and a choice from the species' complete canonical TM learnset; only level-up and TM move sources exist. Dragon's Den validates species moves and the existing AAA-approved Ability pool. Graveyard batches explicit confirmed releases, aggregates consolidated-tier Destroy Value, and grants canonical Curse inventory records. Silph Co. R&D persists separate 2-Ability/4-Move option sets for up to three Pokémon and remains blocking until all choices resolve.

The retired V1 Action-balance suite was removed with its runtime. Current Action/Route coverage is split across `scripts/test-action-operation-contract.js`, `scripts/test-v2-route-browser-mount.js`, `scripts/test-v2-route-runtime-sequences.js`, and the Route engine tests under `versions/next-action-phase/tests/`. Full end-to-end mutation coverage for every surviving location lifecycle is still required before individual effects can be labeled `verifiedComplete`.

## Battle Phase / Teambuilder Integration

The Battle Phase page no longer acts as a second team editor. Teambuilder now presents the submit/lock action and writes revisioned exact-instance snapshots containing Badge assignments, selected Battle Species, legality data, and frozen set information. Battle Phase renders read-only trainer summaries, sanitized mystery/revealed team strips, a six-step phase rail, and viewer-safe matchup strips while preserving revision windows, battle reporting, schedules, records, standings, payouts, and history.

Durable reveal grants support player, selected-player, opponent, public, and host scopes at four explicit reveal levels. Team Preview grants public summary knowledge and exact-instance grants survive refresh and unrelated slot revisions. Hidden Battle Phase markup omits species, sprites, sets, and roster IDs. Full backend payload scoping is not yet implemented, so private full-set reveal effects remain blocked rather than relying on client-side hiding. The focused Battle Phase contract suite contains 6 passing checks.

## Site Shell Game Loading

The website and game render as two visually distinct modes. The global website shell remains outside games; `?view=game` replaces it with one 68px full-screen game header. Its gameplay ribbon reuses canonical state for Live Referee, Action Phase, Team Builder, Player Sheet, Shops, and Battle Phase. The anchored League menu exposes the implemented Standings, MVP Race/Pokemon performance, Pokedex/status history, and Activity Log surfaces. Destination query parameters restore active state through direct links, refresh, and browser back/forward. The focus-trapped right drawer now contains website/profile returns, theme Preferences, account/logout, and permission-gated administration without duplicating gameplay or League navigation. Account identity, backend game context, and `activePlayerId` viewed-trainer context remain separate. The legacy trainer card/player picker is intentionally retained for compatibility.

The legacy phase strip and full-width demo banner are removed. Action Phase now contains the canonical phase agenda, phase-advance button, adjacent theme action, remaining-action count, and a conditional compact Demo explanation.

Game entry now waits behind a League-themed authoritative hydration surface instead of briefly rendering a clean game before the backend snapshot arrives. Failed hydration stays explicit and retryable. The game header exposes `Unsaved`, `Saving`, `Saved`, retryable failure, and revision-conflict states. Full-state saves are single-flight and coalesced, include `expectedVersion`, wait through outstanding Activity response-body version acknowledgement, ignore delayed already-acknowledged timing events, block remote replacement while local changes are dirty, and guard tab exit. Critical phase/gameflow transitions await backend acknowledgement. The backend body ceiling remains 96 MiB for legacy recovery.

Undo storage now uses one shared browser/backend compaction contract. The latest 50 reversible Activity entries retain rollback data; older entries remain visible as History only. Phase-advance undo keeps only changed state instead of repeated full before/after snapshots, terminal interaction records drop duplicate rollback copies, and old saves persist their compact response through the ordinary versioned save queue. Focused tests cover delta preservation, changed-player inventory restoration, bounded retention, terminal interaction cleanup, old-save load migration, stale-version rejection, and orphaned Action destination repair. The real `beta3` save migrated from 45.02 MiB to 4.13 MiB and `beta6` from 94.79 MiB to 1.56 MiB; compressed pre-migration backups are stored under `data/save-compaction-backups/`.

The outer shell now lists authoritative saves through a shared, tolerant game-summary contract and the same runtime API origin used by the inner game. Large saves are scanned with bounded memory and cached by file metadata. Current, legacy, Token/Live Referee-rich, and optionally malformed records remain listable; unsupported schema versions are reported without replacement or migration. The default unprofiled view shows all authoritative games instead of an empty “My Games” result.

Stale game/profile/trainer references are handled independently. Missing game deep links return to the populated library and no longer auto-create blank records. Read-only listing/opening no longer rewrites state or token-art assets. Development diagnostics expose origin, request state, authoritative/normalized/excluded counts, selected identities, rejected IDs/reasons, and schema versions. Known isolated `codex-*` and `browser-smoke` records are excluded from the normal list.

The focused shell suite contains 11 passing unit/integration/static scenarios. Browser validation on port 4175 covered list hydration, exact-ID open, refresh, return/reopen, back/forward, stale-game recovery, backend failure, retry, restart/reconnect, matching Gym/phase, and a clean warning/error console on the repaired bundle. File hashes confirmed zero authoritative game-record changes during list and restart checks.

## Token Scenario Sandbox

The sandbox uses an in-memory working state, private entry baseline, session ID, and authoritative entry revision. Low-level local and backend gameplay writes fail closed while active. Multiplayer events are paused or buffered. Discard reloads authoritative state; revision-aware reconnect catches up if the server advances during exit. Commit uses optimistic concurrency, strips testing/client-only state, preserves suppressed preexisting events, and supports idempotent recovery by session ID.

The sandbox suite currently contains 18 passing infrastructure tests. Validation performed before commit is **structural validation**: object shape, player presence, required major arrays, and JSON serialization. It is not complete Rival Saga legality or state-integrity validation.

Advanced Repair remains experimental. Its snapshot and chronology model is not complete enough for complex live repair.

## Live Referee

Ordered response priority, per-prompt pass/response state, child prompts, parent pause/resume, and targeted-player priority are partially functional. Compatibility metadata such as `canRespondTo` is not fully enforced, effect legality is incomplete, and many resolutions still require Guided or host work.

Live Referee situations now persist an explicit lifecycle separate from legacy Activity status: `pending`, `awaitingResponse`, `awaitingRequiredChoice`, `resolving`, `resolved`, `canceledRefunded`, or `failedRecoveryRequired`. Active records identify the acting player, required action, progression block, resume point, Token-consumption state, and mutation progress. Terminal lifecycle records do not reopen after reload, completed mutations reconcile stale open shells without replay, and interrupted/unsafe resolutions enter host recovery instead of remaining an unexplained open event. The host-only `Cancel Event And Refund` path is available only for recovery-required records and restores the declaration snapshot before one idempotent refund when partial mutation began.

The backend response route now requires `/activity/:id/responses` and cannot fall through to generic Activity creation. Response-shaped records are rejected as standalone situations by both persistence and client normalization. The focused lifecycle suite contains 10 passing scenarios covering the historical misrouting shape, refresh, required choices, stale completed shells, partial rollback, duplicate completion, terminal reload, collapse/reopen, navigation release, and backend nesting.

The floating Live Referee now has a persistent gameplay HUD around Situation + Choices: Series/Gym and canonical phase context, current actor or priority owner, public context chips, compact prompt trail, five stable player positions, contextual player states, a hierarchical Gym/Battle rail, expanded result presentation, a useful collapsed table summary, responsive compact/minimum layouts, keyboard-visible native controls, and reduced-motion fallbacks. Team Lock and Action status are derived from their existing authoritative records; response state uses the current prompt's frozen priority record; battle reporting status is shown when a current schedule exists.

The HUD now supports floating, right/left/bottom docked, expanded, full-table, and collapsed presentations using device-local preferences. Floating bounds reach 96% of the practical viewport; automatic surface-aware allocation, Situation/Table focus, and a persisted pointer/keyboard divider replace the rigid split. Collection and reading screens retain bounded content scrolling with reserved command bars, and compact pentagon mode keeps all five stable seats plus actor, priority, controlled, waiting, confirmation, and alert states. Appearance controls expose density, 90/100/110% scale, and reduced motion without rerendering the surrounding Game Manager or resetting form drafts. Existing single-player target options are mirrored onto the matching pentagon nodes and route back through the existing select/change path; the form remains the fallback and no target legality is added. Lightweight announcements are derived only from existing Action-complete, Team-lock, revision, priority-complete, and effect-result records.

Surviving shared-location Action visits use persisted, idempotent Action operations. Visit confirmation spends the Action once, while the operation keeps the same player and Action number current until its linked current-runtime session and chronology blockers finish. Multi-service locations use the visible Finish Action control and compact authoritative completion route rather than a full-state upload. Route Exploration uses its own current Route Action -> opportunity -> result -> acquisition chain instead of the retired pre-Route encounter/grotto session model. Generic immediate services complete through the shared path, and interaction resolution retries completion when it was waiting on a linked response chain.

Action destination startup recognizes the exact backend reservation that immediately precedes a surviving local location starter, and stale accepted reservations are repaired when their visit flow is gone. Current Route acquisition does not use the retired pre-Route Encounter starter path. Action Phase also exposes a visible Demo Mode toggle and direct trainer selection through the turn rail. New or geometry-free clients default Live Referee to docked mode, with bottom docking on narrow screens. Current focused coverage verifies the shared Action-operation reservation/repair contract separately from Route runtime acquisition coverage.

The effect/Token picker now keeps its title and Back/Cancel navigation outside a bounded internal card scroller. Browser verification covered 37 effects at the desktop overlay and a 520px viewport; the header and footer remained non-overlapping and visible, the collection overflowed internally, and the final effect remained selectable. The focused Action-operation contract suite contains 14 passing lifecycle and wiring tests. These tests cover the operation/turn protocol and static integration points; they are not proof that every location-specific gameplay effect is otherwise complete.

Live Referee controlled-player context now separates Site Shell viewer identity, viewer-linked trainer, current decision owner, and gameplay-controlled trainer. Normal multiplayer remains profile-bound and shows a waiting screen rather than another trainer's effect resources. Free Testing automatically follows unfinished provisional declarations, response priority, pending Trade acceptance, unfinished Action operations, required manual tasks, and Team Build/Lock decisions. Effect inventory, drafts, consumption, event actors, response logs, Trade forms, and the pentagon badge share the same helper; stale navigation is invalidated when the decision owner changes. The focused context suite contains 13 passing scenarios. Beta3 browser reproduction verified that a reloaded Arena Trap declaration previously remained on the wrong controlled trainer and exposed neither continuation nor withdrawal; browser revalidation of the corrected controls is recorded with the fix.

Effect presentation reads actor, target, Token art, prompt ancestry, result announcements, and lingering public statuses from existing state. New in-session prompts can play a short actor-to-target announcement; reload does not replay it. Missing art falls back to a readable badge. This layer is decorative and does not participate in effect resolution.

The phase-boundary offer queue, semantic Control-open query, complete effect legality filtering, redirection, and several battle-payout/reporting details remain incomplete. The HUD only displays those states when an authoritative runtime record already exists; it does not infer or create missing procedures.

The Live Referee Token surface now routes through `getUsableEffectsForContext`. Canonical inventory identity, quantity, runtime usability, declaration-versus-reaction mode, exact timing permission, current targets, and blocking Battle revision operations are checked together. Sabotage visibility is contract-driven through `timingPermissions.sabotageCurseWindow`; unrelated Control, Protection, and Encounter effects do not inherit the Curse window.

Successfully resolved standard Curses that change locked-set legality now create a persisted `WAITING_FOR_REVISIONS` parent operation with exact-instance child tasks. Sabotage, proactive declarations, and Team Preview remain paused until every task passes full-team legality, respects its allowed edit scope, and is explicitly reconfirmed through Teambuilder. The locked snapshot and revision number update only on confirmation. Removed source statuses cancel obsolete tasks from current authoritative state without restoring an old snapshot. This operation flow is implemented for the currently automated standard Curse resolver; future class, perk, and multi-target effects must return equivalent revision requirements before they can use it.

Substitute now has a focused automatic foundation: owned-instance placement, duplicate prevention, interception after response negation and before parent mutation, exact-instance exclusions, persistent species-wide exemptions, and the species-Ban exception. Standard Pokemon-name Curses select one exact Active Roster anchor, then apply to every matching Active Roster instance across players; Arena Curse immunity and Substitute exclude matching instances independently while other matches continue. Extra Ban remains the selected-anchor exception: it checks Substitute only on that anchor, and one selected Substitute negates the universal Ban without touching another matching attachment. Live Referee records affected, protected, and excluded exact instances.

## Token Contract

- Catalog IDs with contract coverage: 45/45.
- Intended Automatic: 30.
- Intended Guided: 15.
- Intended Host Confirmed: 0.
- Verified Complete: 30.
- Partial: 6.
- Text Only: 9.
- Missing: 0.
- Blocked By Ruling: 0.
- Runtime Usable: 35.
- Runtime Guided Only: 9.
- Runtime Development Only: 0.
- Runtime Blocked: 1.

Exclusive runtime reconciliation: 30 `verifiedComplete`, 5 `usablePartial`, 9 `guidedOnly`, 0 `developmentOnly`, 1 `blockedMissingImplementation`, and 0 `blockedRuling`. The implementation-status counts above remain a separate evidence axis.

No resolver is labeled `verifiedComplete` without an effect-specific end-to-end verification test. Contract presence, resolver IDs, mutation descriptions, or accepted Guided text are not completion evidence.

Runtime usability is independent from implementation status. `developmentOnly` and `blocked` Tokens fail before declaration, response recording, consumption, parent mutation, or prompt completion. Foresight Curse is the only currently blocked Token because authenticated source-only move delivery is not implemented. After You, Devolve, Haze Curse, Substitute, Embargo, and Smokescreen remain usable through partial or Guided controllers; unsupported After You interactions fail closed individually. Cold Wave, Wicked Blow, Teleport, Reroll, Honey, and Purge Curse are now `verifiedComplete`. The exact catalog map lives in `TOKEN_IMPLEMENTATION_FINAL_HANDOFF.md`.

## Token QA Coverage

`TOKEN_QA_COVERAGE.md` inventories all Tokens currently marked `partial` and retains the named evidence for Tokens promoted to `verifiedComplete`. It separates result-level Unit, controller Integration, isolated Browser, and Static Wiring evidence from manual observations and missing coverage. Stable `TCF-*`, `TCI-*`, `TSB-*`, and `BROWSER-*` test IDs link report scenarios to executable tests. `npm run test:token-coverage` runs the focused foundation, controller, sandbox, and interactive browser suites; validates every referenced test ID; rejects unsupported `Covered` claims; verifies all current partial Tokens are included; and fails when the generated report is stale.

The current browser suite includes Live Referee Wicked Blow hydration, Sabotage Token authority, exact-inventory 7 Tools/Counterspell chains, provisional Action Control declarations, Extra Encounter persistence/undo, and effect-specific completion scenarios through `BROWSER-030`. The lifecycle slice adds Cold Wave suppression/expiration, Wicked Blow exact-reference evolution, Teleport merged causal undo, Reroll exact-result supersession, Honey acquisition handoff, and Purge post-payout release against temporary authoritative backends. It performs real page loads and refreshes, asserts rendered or production-controller results, and collects uncaught exceptions plus console/log errors. Pure JSON cloning remains Unit evidence, snapshot restoration is not Browser undo, and source ordering is Static Wiring only. These scenarios do not prove normal multiplayer identity across five simultaneous clients.

The 45-Token catalog marks Counterspell, 7 Tools Of The Bandit, Follow Me, and Ditto `verifiedComplete`. A shared exact-inventory runtime preserves source identity, same-Gym temporary copies, two-Gym phase-anchored cooldowns, refresh, phase undo, and one-time expiration/completion History. 7 Tools checks Safeguard before consumption and atomically couples Protection negation to copy delivery. Counterspell restores only the user's exact Token that the current response would negate; the original activation remains negated. Follow Me creates a parent-gated Gym-long relationship that grants canonical real inventory copies after later real Token consumption; virtual activations do not trigger it, Gym end removes the relationship, and earned copies remain. Ditto transforms one exact inventory record into a chosen canonical Token without activating it and preserves exact provenance across refresh and History undo. After You remains non-inventory: supported automatic Control copies use fresh targets and resolve above the original chain, copied Immunity negates original Immunity, and copied Safeguard protects the After You user. Unsupported interactions fail closed individually.

Explicit `isOngoingEffect` records are now the only eligibility source for Lingering Aroma and Cold Wave. Lingering Aroma is `verifiedComplete`: it selects one exact benefiting record, suppresses its original rules, inherits its remaining lifetime, and charges each confirmed outside declaration that targets the beneficiary $500 once; provisional withdrawal pays nothing and later negation does not refund the declaration cost. Cold Wave is `verifiedComplete`: suppression-aware status and Follow Me consumers ignore explicit ongoing effects through Gym end without deleting, expiring, reviving, or rescheduling their sources; duration-only records remain active, refresh and rendered distinction are covered, and causal undo removes only Cold Wave. Smokescreen uses the settled one-spin-per-player replacement rule in a Guided Live Referee screen; it never adds a target and keeps the original target when the wheel returns the original player or no legal corresponding replacement exists.

Move Deleter is `verifiedComplete`: it creates one global canonical-move restriction for the next Gym, and Teambuilder selection, validation, Showdown import, and export reject that move while active; expiration, refresh, and History undo are covered. Purge Curse is `verifiedComplete`: its non-respondable declaration records an immutable brought snapshot, its post-payout checkpoint atomically releases each exact member once, and its causal History undo restores only those records and the exact Token while preserving later unrelated edits. Teleport is `verifiedComplete` for exact root Control-controller Token events: it returns the declaration once at the next Gym's matching phase, revalidates without retargeting, distinguishes gameplay no-effect from system-failure refund, and merges the original delay plus returned mutation into one causal undo without reopening the terminal event. Revenge remains `verifiedComplete` through BROWSER-012.

Reroll and Honey are `verifiedComplete`. Reroll selects one exact unresolved Encounter or supported wheel result, marks the original revision superseded, generates one canonical replacement under a stable operation identity, resumes normal acquisition, rejects stale results before consumption, and causally restores the original result and exact Token. Honey offers an exact finalized-result selector at End of Action, creates a fresh nonrecursive acquisition-ready result containing only canonical species, form, tier, level, and intrinsic rolled properties, completes normal acquisition, and causally removes only its copy and acquired roster record on undo.

Knock Off Curse is `verifiedComplete`. It destroys one exact held Item or TM grant from one exact Active-roster instance. Final TM access loss leaves submitted move data intact, opens exactly one mandatory Sabotage revision from current locked-build provenance, persists through refresh, and reverses exactly through History undo.

Immunity and Extra Encounter Token are also `verifiedComplete`. Immunity owns current-prompt response priority, exact confirmation-time consumption, atomic parent negation, causal results, reload, root undo, and sandbox isolation. Extra Encounter is Action-only, targets one exact chosen player, creates a one-roll Encounter session or adds exactly one roll to that player's open session, persists a stable grant identity, rejects duplicates, opens the normal Encounter flow, restores through History undo, and remains isolated in the Token sandbox.

## Known Dangerous Or Deferred Systems

- Incorrect or incomplete effect-specific timing remains possible despite corrected family defaults.
- Token-consumption outcomes after illegality, cancellation, negation, blocking, no effect, or host rejection need a final lifecycle ruling.
- Sticky Hold is mechanically enforced for the verified Pokemon-targeting Steal Token. Safeguard now has an exact executable matrix for money and Token steal/destroy/copy plus Follow Me and Embargo; Item, TM, Pokemon, forced-payment, and Counterspell-restoration categories remain explicitly outside it.
- Authenticated Foresight delivery remains incomplete and blocked; recursive shared-payload sanitization removes all private records. After You's custom-choice Control and unsupported Protection interactions fail closed individually. Teleport intentionally supports only exact root Token events handled by the Control controller.
- Guided resolvers commonly record text without applying the complete effect.
- Future class/perk effects and nonstandard multi-target Curses are not yet wired to the blocking Sabotage revision operation, even though the parent/child model supports them.
- Substitute's final consumption/refund lifecycle and exact Gym-duration boundary remain unresolved rules questions.
- Restrict is `verifiedComplete` with a six-Gym canonical species restriction, exact Rage-instance immunity, authoritative Teambuilder/submitted-team enforcement, refresh, expiration, causal History undo, and sandbox evidence. Rage Candy Bar is `verifiedComplete` under the current provisional two-Gym counting policy: one structured +3-level/+252-EV/Restrict-immunity record is extended rather than stacked, generated sets consume it, and first/extension undo is causal.
- Extra Ban and Unban are `verifiedComplete`. Extra Ban requires one exact Active anchor and checks Substitute only there before creating an indefinite universal Ban. Unban selects and removes one exact active Ban or Restrict, fails stale choices before consumption, preserves unrelated schedules, and restores the selected status exactly on undo.
- Clear Smog is `verifiedComplete`. It permanently removes only provenance-backed clearable buffs, approved Ability grants, and exact-instance move grants from one Active-roster instance; it preserves native and unrelated state and uses causal History undo.
- Safeguard is `verifiedComplete` for its eight approved categories and explicit non-protected boundaries. Its proactive declaration is response-enabled, exact-player scoped, persistent through refresh, expiring, causally undoable, and sandbox-isolated.
- New Tricks is not implemented. The shared model can represent submitted-team instance restrictions without converting them into species-wide Restrict.
- Arena Trap is `verifiedComplete`. It creates one linked exact-instance record, validates that rival Active-roster target through authoritative Teambuilder bring legality, immediately inserts and locks it in the current Battle Phase draft/team, preserves any minimum Badge assignment, blocks Curse declarations, and creates mandatory repair instead of deleting a member when a team is full. Target eligibility is independent from compensation: targets at least two ordered Battle Tier steps below the Natural tier require their owner to choose one AAA-approved Ability or injectable move before Team Lock. The exact structured grant appears in the generated set, persists through reload, expires and undoes with Arena Trap, and uses the shared customization-ban registry with explicit Trainer Class overrides. Stale confirmed declarations cancel and refund before mutation. Unit, Integration, Result, Browser, reload, undo, expiration, and idempotent sandbox evidence cover the approved lifecycle.
- Incinerate is `verifiedComplete`. It independently selects one exact non-Master-Ball Item or TM record from every rival who has a legal target, atomically destroys those stable-ID records, skips rivals with no legal target, never affects the source player's sheet, and resolves an entirely empty selection with no effect. Immunity negation, atomic failure, exact result summaries, reload, duplicate-completion protection, undo, browser refresh, and sandbox discard/idempotent commit are covered.
- Steal is `verifiedComplete` for its approved Pokemon roster-instance effect. It transfers one exact rival Pokemon to the source player's Active roster, removes stale current Battle Team and active Teambuilder references from the prior owner, records exact previous/new ownership, obeys Sticky Hold, is not blocked by Safeguard's money/Token wording, and is intercepted by Substitute. Reload, undo, stale-target refund, result summaries, browser refresh, and sandbox discard/idempotent commit are covered.
- 7 Tools Of The Bandit is `verifiedComplete`. It can answer only the exact newest Protection response, checks that Protection owner's Safeguard Copy protection before consuming, and resolves atomically: either the Protection is negated and one exact canonical temporary copy is delivered, or neither operation occurs and 7 Tools is refunded. The copy retains its definition/timing/targets, survives refresh, and is removed unused at the next Gym boundary. Counterspell is also `verifiedComplete`: it can answer only a response that would negate the user's own exact Token activation, restores that consumed inventory ID as the explicit consumption exception, and keeps it visible but unusable until the same phase two Gyms later. Phase advance and phase undo preserve both inventory lifecycles.
- Wicked Blow is `verifiedComplete` within its approved boundary. It selects one exact Pokemon from any player's Active Roster, preserves its stable roster-instance identity, applies acquisition-stage and Series Evolution Lock rules, resets species-specific set fields, and updates only existing current-team, locked-slot, and Teambuilder references without creating membership. Non-Active and stale targets fail before mutation; unresolved unlocked branches whose terminal evolutions have different Battle Tiers deliberately fail closed because no canonical tier-selection rule exists. TLS-002, TCI-012, BROWSER-026, and TSB-022/027 cover this boundary, refresh, idempotency, rendered result, and causal undo.
- Toxic Curse, Iron Ball Curse, Flame Curse, Silencing Curse, and Imprison Curse are `verifiedComplete`. Each selects one exact Pokemon from any player's Active Roster, including the source player's own roster, and revalidates stable identity, ownership, Active location, Arena Trap Curse immunity, and response protection before mutation. Canonical Curse contracts include their explicit Sabotage Curse window; ordinary Control and Field effects do not inherit that timing. Forced Items, maximum-two-move enforcement, and neutral-Nature/zero-EV/zero-IV restrictions are effective Teambuilder overlays rather than destructive edits to saved sets. Silencing requires the owner to choose the retained moves before Team Lock. All five expire at the matching phase boundary two Gyms after activation and retain exact History/result, reload, undo, browser-refresh, and sandbox evidence.
- Devolve is `usablePartial`. It rejects missing or ambiguous direct-parent data before consumption, applies a species-wide temporary species/build overlay to unprotected exact Active instances, preserves roster IDs, and restores exact configured records at expiration. Required Team Revision for builds made illegal by the overlay and full production browser refresh/undo/expiration evidence remain incomplete.
- Live Referee final results now persist a structured causal summary containing original effect/actor/target scope, response effects, canonical outcome, mutation/status IDs, exact affected/excluded instances, Token consumption/refund records, continuation, and explicit chain nodes. Gameplay responses use `negated`; administrative cancellation uses `canceledRefunded` and refunds linked Token records. Compact result cards consume this structure and show exact owner/Pokemon labels without exposing roster IDs.
- Ordinary Control Timing now uses one semantic gate for idle Action decisions, Team Building, Shopping, and terminal post-Battle Payout Control. It closes through location operations, interaction chains, required choices, Team Lock, Sabotage, Team Preview, Rival Battles, and unfinished payout. The persisted server-authoritative pre-destination declaration lifecycle remains unchanged: exact effect claims and destination commitments race on the backend, claims consume nothing until confirmation, and withdrawal returns to the same player/action number. Runtime tests `PD-RUNTIME-001` through `PD-RUNTIME-012`, server tests `PD-SERVER-001` through `PD-SERVER-006`, and production browser scenario `BROWSER-009` cover the current foundation.
- The focused Control/Substitute suites cover ownership, inventory deltas, response priority, Immunity ordering, one/multi/species scope, selected-anchor Ban interception, Rage ordering, Unban removal/protection/expiration, Arena Trap validators, Clear Smog provenance, backend reload, controller undo, isolated Browser refresh, and real sandbox discard/commit. Substitute remains `partial` until the universal consumption and Gym-duration conventions are approved and the full production multiplayer path is validated.
- Advanced Repair does not yet own a complete isolated chronology snapshot model.
