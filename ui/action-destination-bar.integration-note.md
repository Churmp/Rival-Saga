Integration target: replace the live V2 Action Phase destination navigator presentation with the approved `rs-action-nav` geometry while preserving the existing hovered/selected destination state and full-screen destination workspace renderer. No new gameplay state is permitted. The live integration should happen only after confirming the current Action Phase DOM/class hooks so the prototype is ported without guessing selectors.